"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { AlertTriangle, ArrowLeft, Building2, CalendarDays, Camera, CheckCircle2, CircleDot, ClipboardCheck, FileImage, History, MapPin, ShieldCheck, Sparkles, Upload, UserRound, Wrench } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { AuditChecklist } from "@/components/audit-checklist";
import { BuildingMap } from "@/components/building-map";
import { DashboardMetric } from "@/components/dashboard-metric";
import { EscalationTimeline } from "@/components/escalation-timeline";
import { EvidenceUploader } from "@/components/evidence-uploader";
import { IssueCard } from "@/components/issue-card";
import { ScoreCard } from "@/components/score-card";
import { StatusBadge } from "@/components/status-badge";
import { VerificationPanel } from "@/components/verification-panel";
import { buildings } from "@/lib/mock-data";
import { useStore } from "@/lib/store";

export function NewAuditView() {
  const [buildingId, setBuildingId] = useState("bld-01"); const { createAuditIssue } = useStore(); const router = useRouter();
  const submit = () => { const issue = createAuditIssue(buildingId); router.push(`/auditor/issues/${issue.id}`); };
  return <AppShell title="New accessibility audit" eyebrow="Auditor workflow"><div className="mb-5 flex items-center justify-between"><Link href="/auditor/audits" className="inline-flex items-center gap-2 text-sm font-bold text-[#617067]"><ArrowLeft size={16} />Audits</Link><span className="rounded-full bg-[#e8f3ee] px-3 py-1.5 text-xs font-black text-[#0b5d45]">AUTO-SAVED LOCALLY</span></div><section className="mb-6 rounded-2xl border border-[#dfe6e1] bg-white p-5"><div className="grid gap-4 lg:grid-cols-[1fr_220px_220px]"><label className="text-sm font-black">Building<select value={buildingId} onChange={(event) => setBuildingId(event.target.value)} className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-white px-4 py-3 font-normal outline-none focus:border-[#0b5d45]">{buildings.map((building) => <option key={building.id} value={building.id}>{building.name}</option>)}</select></label><label className="text-sm font-black">Audit date<input value={new Date().toISOString().slice(0,10)} readOnly className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-[#f6f8f6] px-4 py-3 font-normal" /></label><label className="text-sm font-black">Auditor<input value="Arun Selvan" readOnly className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-[#f6f8f6] px-4 py-3 font-normal" /></label></div></section><div className="mb-5"><p className="text-xs font-black uppercase tracking-[.16em] text-[#0b5d45]">Accessibility checklist</p><h2 className="mt-1 text-2xl font-black">Assess each requirement</h2><p className="mt-1 text-sm text-[#66736c]">Use notes and photos to make findings specific and actionable.</p></div><AuditChecklist onSubmit={submit} /></AppShell>;
}

export function AuditDetailView({ id }: { id: string }) {
  return <AppShell title={id} eyebrow="Completed audit"><section className="grid gap-5 lg:grid-cols-[1fr_320px]"><div className="rounded-2xl border border-[#dfe6e1] bg-white p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-[#0b5d45]">Government General Hospital</p><h2 className="mt-1 text-2xl font-black">Accessibility audit summary</h2><p className="mt-2 text-sm text-[#66736c]">Completed by Arun Selvan · 29 August 2026</p></div><StatusBadge value="COMPLETED" /></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-emerald-50 p-4"><p className="text-xs font-bold text-emerald-800">Compliant</p><p className="mt-1 text-2xl font-black">15</p></div><div className="rounded-xl bg-amber-50 p-4"><p className="text-xs font-bold text-amber-800">Partial</p><p className="mt-1 text-2xl font-black">3</p></div><div className="rounded-xl bg-red-50 p-4"><p className="text-xs font-bold text-red-800">Not compliant</p><p className="mt-1 text-2xl font-black">2</p></div></div><h3 className="mt-7 font-black">Issues created from findings</h3><div className="mt-3 space-y-3">{["Accessible ramp is not provided","Tactile route ends before reception","Parking signage is faded"].map((title,index) => <div key={title} className="flex items-center justify-between rounded-xl border border-[#dfe6e1] p-4"><div><p className="text-xs font-black text-[#0b5d45]">ENTRANCE · ACC-{1240+index}</p><p className="mt-1 font-bold">{title}</p></div><StatusBadge value={index===0?"HIGH":"MEDIUM"} /></div>)}</div></div><div className="space-y-5"><ScoreCard score={82} /><div className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><h3 className="font-black">Audit record</h3><dl className="mt-4 space-y-4 text-sm"><div><dt className="text-[#748078]">Audit type</dt><dd className="font-bold">Full accessibility audit</dd></div><div><dt className="text-[#748078]">Total criteria</dt><dd className="font-bold">20 requirements</dd></div><div><dt className="text-[#748078]">Evidence</dt><dd className="font-bold">8 photos attached</dd></div></dl></div></div></section></AppShell>;
}

function IssueTimeline({ status }: { status: string }) {
  const steps = ["Issue created", "Assigned", "In progress", "Evidence uploaded", "Verification", "Closed"];
  const activeIndex = status === "PENDING" ? 1 : status === "IN_PROGRESS" || status === "REWORK_REQUIRED" ? 2 : status === "VERIFICATION_PENDING" ? 4 : status === "CLOSED" || status === "VERIFIED" ? 5 : 1;
  return <ol className="grid gap-2 sm:grid-cols-6">{steps.map((step,index) => <li key={step} className="flex items-center gap-2 sm:block"><span className={`grid h-7 w-7 shrink-0 place-items-center rounded-full text-xs font-black ${index <= activeIndex ? "bg-[#0b5d45] text-white" : "bg-[#e7ece9] text-[#7d8982]"}`}>{index < activeIndex ? "✓" : index+1}</span><p className={`text-xs font-bold sm:mt-2 ${index <= activeIndex ? "text-[#284c3e]" : "text-[#89958e]"}`}>{step}</p></li>)}</ol>;
}

export function IssueDetailView({ id, mode }: { id: string; mode: "admin" | "auditor" | "manager" }) {
  const { issues, assignIssue, startWork, submitEvidence, verifyIssue } = useStore(); const issue = issues.find((item) => item.id === id) ?? issues[0]; const [responsible, setResponsible] = useState(issue.responsible === "Unassigned" ? "Kavitha Mani — Hospital Facilities Manager" : issue.responsible); const [department, setDepartment] = useState(issue.department === "Unassigned" ? "Hospital Engineering" : issue.department); const [deadline, setDeadline] = useState(issue.deadline === "Not set" ? "2026-09-28" : issue.deadline); const [assigned, setAssigned] = useState(false); const router = useRouter();
  const back = mode === "manager" ? "/manager/issues" : mode === "auditor" ? "/auditor/issues" : "/admin/issues";
  const decision = (approved: boolean, comment: string) => { verifyIssue(issue.id, approved, comment); router.push(mode === "auditor" ? "/auditor/verification" : back); };
  return <AppShell title={issue.id} eyebrow="Accessibility issue"><div className="mb-5 flex items-center justify-between"><Link href={back} className="inline-flex items-center gap-2 text-sm font-bold text-[#617067]"><ArrowLeft size={16} />Back to issues</Link>{(issue.status === "OVERDUE" || issue.status === "ESCALATED") && <span className="flex items-center gap-2 rounded-xl bg-red-100 px-3 py-2 text-xs font-black text-red-800"><AlertTriangle size={15} />OVERDUE · ESCALATION LEVEL {Math.max(issue.escalationLevel,1)}</span>}</div><section className="rounded-2xl border border-[#dfe6e1] bg-white p-5 sm:p-7"><div className="flex flex-col justify-between gap-5 lg:flex-row"><div className="max-w-3xl"><div className="flex flex-wrap gap-2"><StatusBadge value={issue.status} /><StatusBadge value={issue.severity} /><span className="rounded-full bg-[#edf1ee] px-3 py-1 text-xs font-black text-[#5b6961]">{issue.category}</span></div><h2 className="mt-4 text-3xl font-black tracking-tight">{issue.title}</h2><p className="mt-3 leading-7 text-[#5e6c64]">{issue.description}</p></div><div className="min-w-[250px] rounded-xl bg-[#f3f6f4] p-4"><p className="flex items-center gap-2 text-sm font-black"><Building2 size={17} className="text-[#0b5d45]" />{issue.buildingName}</p><p className="mt-3 flex items-center gap-2 text-xs text-[#66736c]"><CalendarDays size={15} />Deadline: <strong className="text-[#17231d]">{issue.deadline}</strong></p><p className="mt-2 flex items-center gap-2 text-xs text-[#66736c]"><UserRound size={15} />{issue.responsible}</p></div></div><div className="mt-7 border-t border-[#e5eae7] pt-6"><IssueTimeline status={issue.status} /></div></section><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_380px]"><div className="space-y-5"><section className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><p className="text-xs font-black uppercase tracking-wider text-[#0b5d45]">Required corrective action</p><p className="mt-2 font-bold leading-7">{issue.actionRequired}</p></section>{mode === "admin" && <section className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><h2 className="text-lg font-black">Assign responsibility and deadline</h2><p className="mt-1 text-sm text-[#66736c]">Responsibility is confirmed by an administrator; AI suggestions are editable.</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Responsible person<select value={responsible} onChange={(event) => setResponsible(event.target.value)} className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-white px-3 py-3 font-normal"><option>Kavitha Mani — Hospital Facilities Manager</option><option>Ramesh Iyer — Building Maintenance Lead</option></select></label><label className="text-sm font-bold">Department<select value={department} onChange={(event) => setDepartment(event.target.value)} className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-white px-3 py-3 font-normal"><option>Hospital Engineering</option><option>Building Administration</option><option>Public Works</option></select></label><label className="text-sm font-bold">Deadline<input type="date" value={deadline} onChange={(event) => setDeadline(event.target.value)} className="mt-2 w-full rounded-xl border border-[#c9d4ce] px-3 py-3 font-normal" /></label><label className="text-sm font-bold">Required action<input value={issue.actionRequired} readOnly className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-[#f4f6f4] px-3 py-3 font-normal" /></label></div><button onClick={() => { assignIssue(issue.id, responsible, department, deadline); setAssigned(true); }} className="mt-5 w-full rounded-xl bg-[#0b5d45] px-5 py-3.5 font-black text-white">{assigned ? "Assignment saved ✓" : "Assign issue"}</button></section>}{mode === "manager" && issue.status === "PENDING" && <section className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><h2 className="text-lg font-black">Ready to start?</h2><p className="mt-1 text-sm text-[#66736c]">Starting work updates the accountability timeline. You still cannot verify or close the issue.</p><button onClick={() => startWork(issue.id)} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-[#0b5d45] px-5 py-3.5 font-black text-white"><Wrench size={18} />Start work</button></section>}{mode === "manager" && ["IN_PROGRESS","REWORK_REQUIRED"].includes(issue.status) && <EvidenceUploader onSubmit={(description,fileName) => submitEvidence(issue.id, description, fileName)} />}{(mode === "auditor" || mode === "admin") && issue.status === "VERIFICATION_PENDING" && <VerificationPanel onDecision={decision} />}{issue.status === "CLOSED" && <section className="rounded-2xl border border-emerald-200 bg-emerald-50 p-6"><div className="flex items-center gap-3"><CheckCircle2 className="text-emerald-700" /><div><h2 className="font-black text-emerald-900">Verified and closed</h2><p className="mt-1 text-sm text-emerald-800">A human verifier approved the evidence. The accessibility score is updated.</p></div></div>{issue.verifierComment && <p className="mt-4 rounded-xl bg-white/70 p-4 text-sm font-bold text-emerald-900">“{issue.verifierComment}”</p>}</section>}<section className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><h2 className="flex items-center gap-2 font-black"><History size={18} />Evidence and activity</h2>{issue.evidence.length ? <div className="mt-4 grid gap-3 sm:grid-cols-2">{issue.evidence.map((item) => <div key={item.id} className="rounded-xl border border-[#dfe6e1] p-4"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e9f3ee] text-[#0b5d45]"><FileImage size={20} /></span><p className="mt-3 text-sm font-black">{item.fileName}</p><p className="mt-1 text-xs leading-5 text-[#66736c]">{item.description}</p><p className="mt-2 text-[10px] font-bold uppercase tracking-wider text-[#85918a]">{item.type} evidence</p></div>)}</div> : <div className="mt-4 rounded-xl border-2 border-dashed border-[#d5ddd8] p-7 text-center"><Upload className="mx-auto text-[#91a099]" /><p className="mt-2 text-sm font-bold">No completion evidence yet</p></div>}</section></div><aside className="space-y-5"><div className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><h2 className="font-black">Original audit finding</h2><div className="mt-4 grid h-44 place-items-center rounded-xl bg-[linear-gradient(135deg,#dce8e1,#f4e1d8)] text-center"><div><Camera className="mx-auto text-[#587468]" /><p className="mt-2 text-xs font-bold text-[#526159]">Entrance audit photo</p><p className="text-[10px] text-[#718078]">Steps visible · no ramp</p></div></div><p className="mt-4 text-xs font-black uppercase tracking-wider text-[#b33c25]">Not compliant</p><p className="mt-1 text-sm text-[#5f6d65]">Wheelchair users cannot enter independently from the public route.</p></div><EscalationTimeline level={issue.escalationLevel} />{issue.evidence.length > 0 && <div className="rounded-2xl border border-[#cce0d6] bg-[#f2f8f5] p-5"><p className="flex items-center gap-2 text-xs font-black uppercase tracking-wider text-[#0b5d45]"><Sparkles size={15} />AI advisory</p><p className="mt-2 text-sm font-bold">Visible structural change detected.</p><p className="mt-1 text-xs text-[#66736c]">Human verification required. AI cannot close this issue.</p></div>}</aside></div></AppShell>;
}

export function VerificationView({ issueId }: { issueId?: string }) {
  const { issues } = useStore(); const pending = issues.filter((item) => item.status === "VERIFICATION_PENDING");
  if (issueId) return <IssueDetailView id={issueId} mode="auditor" />;
  return <AppShell title="Evidence verification" eyebrow="Auditor human review"><section className="rounded-2xl bg-[#12382d] p-6 text-white"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-xl bg-white/10"><ShieldCheck /></span><div><h2 className="text-xl font-black">Humans make the final decision</h2><p className="mt-1 text-sm text-[#c9ddd5]">Compare the finding, required action and evidence. AI advice never determines compliance.</p></div></div></section><div className="mt-6 grid gap-4 lg:grid-cols-2">{pending.length ? pending.map((issue) => <IssueCard key={issue.id} issue={issue} href={`/auditor/verification?issue=${issue.id}`} />) : <div className="col-span-full rounded-2xl border-2 border-dashed border-[#cad5cf] bg-white p-12 text-center"><CheckCircle2 className="mx-auto text-emerald-600" size={36} /><h2 className="mt-4 text-xl font-black">Verification queue is clear</h2><p className="mt-2 text-sm text-[#66736c]">Use the manager role to submit evidence, then return here.</p></div>}</div></AppShell>;
}

export function AdminBuildingDetailView({ id }: { id: string }) {
  const { issues, reports } = useStore(); const building = buildings.find((item) => item.id === id) ?? buildings[0]; const buildingIssues = issues.filter((item) => item.buildingId === building.id); const open = buildingIssues.filter((item) => !["CLOSED","VERIFIED"].includes(item.status));
  return <AppShell title={building.name} eyebrow="Building management" action={<button className="rounded-xl border border-[#c8d5ce] bg-white px-4 py-2.5 text-sm font-bold">Edit building</button>}><div className="grid gap-5 xl:grid-cols-[1fr_360px]"><div className="space-y-5"><section className="rounded-2xl border border-[#dfe6e1] bg-white p-6"><div className="flex flex-col justify-between gap-5 sm:flex-row"><div><div className="flex gap-2"><StatusBadge value={building.ownership} /><StatusBadge value={building.type} /></div><p className="mt-4 flex items-center gap-2 text-sm text-[#66736c]"><MapPin size={16} />{building.address}, Chennai</p><p className="mt-2 text-sm text-[#66736c]">{building.organisation}</p></div><div className="w-full sm:w-[280px]"><ScoreCard score={building.score} compact /></div></div></section><section className="grid gap-4 sm:grid-cols-4"><DashboardMetric label="Total issues" value={buildingIssues.length} detail="All findings" icon={ClipboardCheck} /><DashboardMetric label="Open" value={open.length} detail="Need action" icon={CircleDot} tone="orange" /><DashboardMetric label="Overdue" value={buildingIssues.filter((item)=>item.status==="OVERDUE").length} detail="Escalation due" icon={AlertTriangle} tone="red" /><DashboardMetric label="Reports" value={reports.filter((item)=>item.buildingId===id).length} detail="Citizen input" icon={UserRound} tone="blue" /></section><section><h2 className="mb-4 text-xl font-black">Recent issues</h2><div className="grid gap-3 lg:grid-cols-2">{buildingIssues.slice(0,6).map((issue) => <IssueCard key={issue.id} issue={issue} href={`/admin/issues/${issue.id}`} />)}</div></section><section><h2 className="mb-4 text-xl font-black">Location</h2><BuildingMap /></section></div><aside className="space-y-5"><section className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><h2 className="font-black">Accessibility checklist</h2><div className="mt-4 space-y-3">{[["Accessible entrance",building.features.entrance],["Ramp",building.features.ramp==="Available"],["Accessible toilet",building.features.toilet],["Accessible parking",building.features.parking]].map(([label,ok]) => <div key={label as string} className="flex items-center justify-between rounded-xl bg-[#f4f7f5] p-3 text-sm font-bold"><span>{label}</span>{ok?<CheckCircle2 size={18} className="text-emerald-600"/>:<AlertTriangle size={18} className="text-amber-600"/>}</div>)}</div></section><section className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><h2 className="font-black">Audit history</h2><div className="mt-4 space-y-4">{[["2026-07-03",building.score],["2025-12-12",65],["2025-05-19",45]].map(([date,score])=><div key={date} className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-full bg-[#e8f3ee] text-[#0b5d45]"><ClipboardCheck size={16}/></span><div className="flex-1"><p className="text-sm font-bold">Full building audit</p><p className="text-xs text-[#748078]">{date}</p></div><strong>{score}/100</strong></div>)}</div></section></aside></div></AppShell>;
}
