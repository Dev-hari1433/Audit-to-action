"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useMemo, useState } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
import { ArrowLeft, Building2, Camera, CheckCircle2, LocateFixed, MapPin, Mic, Search, ShieldCheck, Sparkles, UserRound } from "lucide-react";
import { BuildingCard } from "@/components/building-card";
import { BuildingMap } from "@/components/building-map";
import { IssueCard } from "@/components/issue-card";
import { ScoreCard } from "@/components/score-card";
import { buildings, demoAccounts } from "@/lib/mock-data";
import { useStore } from "@/lib/store";
import type { Role } from "@/types";

const roleHome: Record<Role, string> = { CITIZEN: "/citizen/dashboard", AUDITOR: "/auditor/dashboard", BUILDING_MANAGER: "/manager/dashboard", ADMIN: "/admin/dashboard" };
const roleLabels: Record<Role, string> = { CITIZEN: "Citizen", AUDITOR: "Auditor", BUILDING_MANAGER: "Manager", ADMIN: "Administrator" };

function PublicHeader() {
  return <header className="border-b border-[#dfe6e1] bg-white"><div className="mx-auto flex h-[70px] max-w-7xl items-center justify-between px-5 sm:px-8"><Link href="/" className="flex items-center gap-3 font-black"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#0b5d45] text-white">A</span><span>ACCESS<span className="text-[#d95425]">TRACK</span></span></Link><nav className="flex items-center gap-2 text-sm font-bold"><Link className="hidden rounded-xl px-4 py-2.5 hover:bg-[#f2f6f3] sm:block" href="/buildings">Buildings</Link><Link className="rounded-xl bg-[#12382d] px-4 py-2.5 text-white" href="/login">Demo login</Link></nav></div></header>;
}

export function LoginView() {
  const { login, role } = useStore();
  const router = useRouter();
  const selectRole = (nextRole: Role) => { login(nextRole); router.push(roleHome[nextRole]); };
  return (
    <main className="min-h-screen bg-[#f3f6f4]"><PublicHeader /><div className="mx-auto grid max-w-6xl gap-8 px-5 py-10 sm:px-8 lg:grid-cols-[.8fr_1.2fr] lg:py-16"><section className="rounded-[2rem] bg-[#12382d] p-7 text-white sm:p-10"><p className="text-xs font-black uppercase tracking-[.18em] text-[#9fc8b9]">Demo ready</p><h1 className="mt-4 text-4xl font-black tracking-tight">See accountability from every side.</h1><p className="mt-4 leading-7 text-[#c9ddd5]">Switch between roles without credentials. Every action is saved locally, so you can demonstrate the complete audit-to-closure journey.</p><div className="mt-8 space-y-3">{["Create an audit finding", "Assign responsibility and deadline", "Upload completion evidence", "Approve or request rework"].map((text, index) => <div key={text} className="flex items-center gap-3 rounded-xl bg-white/10 p-3 text-sm font-bold"><span className="grid h-7 w-7 place-items-center rounded-full bg-[#ff7840] text-xs">{index + 1}</span>{text}</div>)}</div><p className="mt-8 text-xs text-[#9fc8b9]">Supabase is used when configured. This demo safely falls back to seeded local data.</p></section><section className="rounded-[2rem] border border-[#dfe6e1] bg-white p-6 shadow-[0_18px_55px_rgba(20,45,34,.08)] sm:p-9"><p className="text-xs font-black uppercase tracking-[.18em] text-[#0b5d45]">{role ? `Currently: ${roleLabels[role]}` : "Choose a workspace"}</p><h2 className="mt-2 text-3xl font-black">Login to demo mode</h2><p className="mt-2 text-sm text-[#6b7870]">No email or password required.</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{demoAccounts.map((account) => <button key={account.role} onClick={() => selectRole(account.role)} className="group rounded-2xl border border-[#dfe6e1] p-5 text-left hover:border-[#0b5d45] hover:bg-[#f4faf7]"><div className="flex items-start justify-between"><span className="rounded-xl bg-[#e8f3ee] p-2.5 text-[#0b5d45]">{account.role === "ADMIN" ? <ShieldCheck size={20} /> : <UserRound size={20} />}</span><span className="text-xl text-[#a0aba5] group-hover:translate-x-1 group-hover:text-[#0b5d45]">→</span></div><p className="mt-4 text-xs font-black uppercase tracking-wider text-[#0b5d45]">Login as {roleLabels[account.role]}</p><p className="mt-1 text-lg font-extrabold">{account.name}</p><p className="mt-1 text-xs text-[#738078]">{account.detail}</p></button>)}</div><div className="mt-6 flex justify-between text-sm"><Link href="/register" className="font-bold text-[#0b5d45] hover:underline">Create a citizen account</Link><Link href="/" className="font-bold text-[#65736b] hover:underline">Back to home</Link></div></section></div></main>
  );
}

const reportSchema = z.object({ buildingId: z.string().min(1, "Choose a building"), category: z.string().min(1), description: z.string().min(10, "Please add a little more detail"), location: z.string().min(2, "Add where you saw the barrier"), contact: z.string().email("Enter a valid email").or(z.literal("")) });
type ReportForm = z.infer<typeof reportSchema>;

export function ReportProblemView() {
  const { submitReport } = useStore();
  const [submittedId, setSubmittedId] = useState("");
  const [fileName, setFileName] = useState("");
  const [fileError, setFileError] = useState("");
  const [analysis, setAnalysis] = useState(false);
  const [listening, setListening] = useState(false);
  const { register, handleSubmit, setValue, formState: { errors } } = useForm<ReportForm>({ resolver: zodResolver(reportSchema), defaultValues: { buildingId: "bld-01", category: "RAMP", description: "", location: "", contact: "" } });
  const onSubmit = (values: ReportForm) => setSubmittedId(submitReport(values).id);
  const useLocation = () => navigator.geolocation?.getCurrentPosition(() => setValue("location", "Main entrance — current location attached"), () => setValue("location", "Main entrance"));
  const startVoice = () => {
    const SpeechRecognition = (window as unknown as { webkitSpeechRecognition?: new () => { lang: string; start: () => void; onresult: (event: { results: { 0: { transcript: string } }[] }) => void; onend: () => void } }).webkitSpeechRecognition;
    if (!SpeechRecognition) { setValue("description", "There is no wheelchair ramp at the main entrance of Government General Hospital."); setValue("location", "Main entrance"); return; }
    const recognition = new SpeechRecognition(); recognition.lang = "en-IN"; setListening(true); recognition.onresult = (event) => { setValue("description", event.results[0][0].transcript); setValue("location", "Main entrance"); }; recognition.onend = () => setListening(false); recognition.start();
  };
  if (submittedId) return <main className="min-h-screen bg-[#f3f6f4]"><PublicHeader /><div className="mx-auto max-w-2xl px-5 py-16"><section className="rounded-[2rem] border border-[#cde2d7] bg-white p-7 text-center shadow-lg sm:p-10"><span className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-emerald-100 text-emerald-700"><CheckCircle2 size={34} /></span><p className="mt-5 text-xs font-black uppercase tracking-wider text-[#0b5d45]">Report submitted</p><h1 className="mt-2 text-3xl font-black">{submittedId}</h1><p className="mx-auto mt-3 max-w-md text-[#66736c]">Your report is now in the accessibility review queue. Keep the report ID to track progress.</p><div className="mt-7 grid grid-cols-3 gap-2 text-xs font-bold sm:grid-cols-7">{["Submitted", "Review", "Accepted", "Assigned", "In progress", "Verified", "Resolved"].map((step, index) => <div key={step} className={`rounded-xl p-2 ${index === 0 ? "bg-[#0b5d45] text-white" : "bg-[#edf1ee] text-[#718078]"}`}>{step}</div>)}</div><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link href="/citizen/reports" className="rounded-xl bg-[#0b5d45] px-5 py-3 font-bold text-white">Track this report</Link><button onClick={() => setSubmittedId("")} className="rounded-xl border border-[#ccd6d0] px-5 py-3 font-bold">Submit another</button></div></section></div></main>;
  return (
    <main className="min-h-screen bg-[#f3f6f4]"><PublicHeader /><div className="mx-auto grid max-w-6xl gap-7 px-5 py-8 sm:px-8 lg:grid-cols-[1fr_360px]"><section><Link href="/" className="inline-flex items-center gap-2 text-sm font-bold text-[#5e6c64]"><ArrowLeft size={16} />Back</Link><p className="mt-6 text-xs font-black uppercase tracking-[.18em] text-[#0b5d45]">Takes less than one minute</p><h1 className="mt-2 text-4xl font-black tracking-tight">Report accessibility problem</h1><p className="mt-2 text-[#66736c]">Tell us what is blocking access. You can remain anonymous.</p><form onSubmit={handleSubmit(onSubmit)} className="mt-7 space-y-5 rounded-[2rem] border border-[#dfe6e1] bg-white p-5 shadow-[0_14px_40px_rgba(20,45,34,.06)] sm:p-8"><label className="block text-sm font-extrabold">Select building<select {...register("buildingId")} className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-white px-4 py-3.5 font-normal outline-none focus:border-[#0b5d45] focus:ring-4 focus:ring-[#0b5d45]/10">{buildings.map((building) => <option key={building.id} value={building.id}>{building.name}</option>)}</select></label><label className="block text-sm font-extrabold">What is the problem?<select {...register("category")} className="mt-2 w-full rounded-xl border border-[#c9d4ce] bg-white px-4 py-3.5 font-normal outline-none focus:border-[#0b5d45]"><option value="RAMP">Ramp / entrance</option><option value="TOILETS">Accessible toilet</option><option value="PATHWAY">Pathway / movement</option><option value="PARKING">Accessible parking</option><option value="SIGNAGE">Signage / communication</option><option value="OTHER">Other barrier</option></select></label><label className="block text-sm font-extrabold">Describe the barrier<textarea {...register("description")} rows={4} placeholder="Example: There are three steps at the main entrance and no ramp." className="mt-2 w-full rounded-xl border border-[#c9d4ce] px-4 py-3.5 font-normal outline-none focus:border-[#0b5d45] focus:ring-4 focus:ring-[#0b5d45]/10" />{errors.description && <span className="mt-1 block text-xs font-bold text-red-700">{errors.description.message}</span>}</label><div className="grid gap-3 sm:grid-cols-2"><label className="flex cursor-pointer items-center justify-center gap-2 rounded-xl border-2 border-dashed border-[#b9cdc2] bg-[#f6faf8] p-4 text-sm font-bold text-[#0b5d45]"><Camera size={18} />{fileName || "Upload photo"}<input className="sr-only" type="file" accept="image/jpeg,image/png,image/webp" onChange={(event) => { const file = event.target.files?.[0]; if (file && file.size > 5_000_000) { setFileError("Photo must be under 5 MB"); setFileName(""); } else { setFileError(""); setFileName(file?.name ?? ""); } }} /></label><button type="button" onClick={useLocation} className="flex items-center justify-center gap-2 rounded-xl border border-[#c9d4ce] p-4 text-sm font-bold hover:border-[#0b5d45]"><LocateFixed size={18} />Use my location</button></div>{fileError && <p className="text-xs font-bold text-red-700">{fileError}</p>}<label className="block text-sm font-extrabold">Where is it?<input {...register("location")} placeholder="Main entrance, ground floor, parking area…" className="mt-2 w-full rounded-xl border border-[#c9d4ce] px-4 py-3.5 font-normal outline-none focus:border-[#0b5d45]" />{errors.location && <span className="mt-1 block text-xs font-bold text-red-700">{errors.location.message}</span>}</label><button type="button" onClick={startVoice} className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#fff3ed] px-4 py-4 font-extrabold text-[#bd481d] hover:bg-[#ffe8dc]"><Mic size={20} />{listening ? "Listening…" : "Report by voice"}</button><button type="button" onClick={() => setAnalysis(true)} className="flex w-full items-center justify-center gap-2 rounded-xl border border-[#c8d9d0] px-4 py-3 text-sm font-bold text-[#0b5d45]"><Sparkles size={17} />Analyze photo with AI <span className="font-normal text-[#748078]">(optional)</span></button>{analysis && <div className="rounded-xl border border-[#c9dfd4] bg-[#eff8f3] p-4 text-sm"><p className="font-black text-[#0b5d45]">AI preliminary analysis</p><ul className="mt-2 list-disc space-y-1 pl-5 text-[#526159]"><li>Steps detected at entrance</li><li>No visible ramp</li><li>Possible lack of handrail</li></ul><p className="mt-2 font-bold">Confidence: 87%</p><p className="mt-2 text-xs font-bold text-[#7a4c22]">AI result is advisory. Human verification is required.</p></div>}<details className="rounded-xl bg-[#f4f6f4] p-4"><summary className="cursor-pointer text-sm font-bold">Add contact information (optional)</summary><label className="mt-3 block text-xs font-bold">Email<input {...register("contact")} type="email" className="mt-1 w-full rounded-xl border border-[#c9d4ce] bg-white px-4 py-3 font-normal" /></label></details><button type="submit" className="w-full rounded-xl bg-[#e15f2a] px-5 py-4 text-lg font-black text-white shadow-[0_10px_25px_rgba(225,95,42,.22)] hover:bg-[#c94f1f]">Submit report</button></form></section><aside className="space-y-4 lg:pt-[108px]"><div className="rounded-2xl bg-[#12382d] p-6 text-white"><h2 className="font-black">What happens next?</h2><ol className="mt-4 space-y-4 text-sm">{["Accessibility team reviews the report", "A responsible department is assigned", "Evidence and human verification are required", "You can track progress with your report ID"].map((text, index) => <li key={text} className="flex gap-3"><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-white/15 text-xs font-black">{index + 1}</span><span className="text-[#d2e2dc]">{text}</span></li>)}</ol></div><div className="rounded-2xl border border-[#dfe6e1] bg-white p-5 text-sm text-[#66736c]"><p className="font-black text-[#17231d]">Privacy note</p><p className="mt-2 leading-6">Public profiles never expose personal contact information. Photos are used only to review the accessibility barrier.</p></div></aside></div></main>
  );
}

export function BuildingsView() {
  const [query, setQuery] = useState("");
  const filtered = useMemo(() => buildings.filter((building) => `${building.name} ${building.type} ${building.city}`.toLowerCase().includes(query.toLowerCase())), [query]);
  return <main className="min-h-screen bg-[#f3f6f4]"><PublicHeader /><section className="border-b border-[#dfe6e1] bg-[#12382d] py-12 text-white"><div className="mx-auto max-w-7xl px-5 sm:px-8"><p className="text-xs font-black uppercase tracking-[.18em] text-[#a8c9bd]">Public directory · Chennai pilot</p><h1 className="mt-2 text-4xl font-black">Building accessibility</h1><p className="mt-3 max-w-2xl text-[#cce0d8]">Clear monitoring scores, known barriers and verified improvements. Scores are for accountability—not legal certification.</p><label className="mt-7 flex max-w-xl items-center gap-3 rounded-xl bg-white px-4 py-3.5 text-[#17231d]"><Search size={19} className="text-[#6e7b73]" /><span className="sr-only">Search buildings</span><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full outline-none" placeholder="Search by building, type or area" /></label></div></section><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><div className="mb-6 flex items-center justify-between"><p className="text-sm font-bold text-[#65736b]">{filtered.length} monitored buildings</p><div className="flex items-center gap-3 text-xs font-bold"><span><i className="mr-1 inline-block h-2.5 w-2.5 rounded-full bg-emerald-600" />Good</span><span><i className="mr-1 inline-block h-2.5 w-2.5 rounded-full bg-amber-500" />Needs work</span><span><i className="mr-1 inline-block h-2.5 w-2.5 rounded-full bg-red-600" />Priority</span></div></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map((building) => <BuildingCard key={building.id} building={building} />)}</div></div></main>;
}

export function BuildingDetailView({ id }: { id: string }) {
  const { issues } = useStore(); const building = buildings.find((item) => item.id === id) ?? buildings[0]; const buildingIssues = issues.filter((issue) => issue.buildingId === building.id).slice(0, 4);
  return <main className="min-h-screen bg-[#f3f6f4]"><PublicHeader /><section className="border-b border-[#dfe6e1] bg-white"><div className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><Link href="/buildings" className="inline-flex items-center gap-2 text-sm font-bold text-[#607067]"><ArrowLeft size={16} />All buildings</Link><div className="mt-6 flex flex-col justify-between gap-6 lg:flex-row lg:items-end"><div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-[#e8f3ee] px-3 py-1 text-xs font-bold text-[#0b5d45]">{building.type}</span><span className="rounded-full bg-[#edf0ee] px-3 py-1 text-xs font-bold text-[#5d6a63]">{building.ownership}</span></div><h1 className="mt-3 text-4xl font-black tracking-tight">{building.name}</h1><p className="mt-2 flex items-center gap-2 text-[#65736b]"><MapPin size={17} />{building.address}, {building.city}</p></div><div className="w-full lg:w-[320px]"><ScoreCard score={building.score} compact /></div></div></div></section><div className="mx-auto grid max-w-7xl gap-6 px-5 py-8 sm:px-8 lg:grid-cols-[1fr_360px]"><section className="space-y-6"><div className="rounded-2xl border border-[#dfe6e1] bg-white p-6"><h2 className="text-xl font-black">Accessibility at a glance</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[["Accessible entrance", building.features.entrance ? "Yes" : "No"],["Ramp", building.features.ramp],["Accessible toilet", building.features.toilet ? "Yes" : "No"],["Accessible parking", building.features.parking ? "Yes" : "No"]].map(([label,value]) => <div key={label as string} className="flex items-center justify-between rounded-xl bg-[#f4f7f5] p-4"><span className="text-sm font-bold text-[#5e6c64]">{label}</span><span className="text-sm font-black">{value}</span></div>)}</div><p className="mt-5 text-xs font-bold text-[#7d8982]">Last accessibility audit: {building.lastAudit}</p></div><div><div className="mb-4 flex items-end justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-[#0b5d45]">Public accountability</p><h2 className="mt-1 text-xl font-black">Known open issues</h2></div><Link href="/report" className="text-sm font-bold text-[#d95425]">Report a problem</Link></div><div className="grid gap-3">{buildingIssues.map((issue) => <IssueCard key={issue.id} issue={issue} href={`/admin/issues/${issue.id}`} />)}</div></div><div><h2 className="mb-4 text-xl font-black">Location</h2><BuildingMap /></div></section><aside className="space-y-5"><div className="rounded-2xl border border-[#dfe6e1] bg-white p-5"><p className="text-xs font-black uppercase tracking-wider text-[#6a776f]">Score improvement</p><div className="mt-4 flex items-end justify-between gap-2">{[45,65,building.score].map((score,index) => <div key={score} className="flex-1 text-center"><div className="mx-auto w-full rounded-t-lg bg-[#bcd7ca]" style={{ height: `${score * 1.1}px`, background: index === 2 ? "#0b5d45" : undefined }} /><p className="mt-2 text-lg font-black">{score}</p><p className="text-[10px] text-[#77837c]">Audit {index + 1}</p></div>)}</div><p className="mt-5 text-xs text-[#718078]">Monitoring score — not a legal certification.</p></div><div className="rounded-2xl bg-[#12382d] p-6 text-white"><Building2 size={24} /><h2 className="mt-4 text-lg font-black">See a barrier here?</h2><p className="mt-2 text-sm leading-6 text-[#cbded7]">Send a photo and location in under one minute. Reports are tracked until resolution.</p><Link href="/report" className="mt-5 block rounded-xl bg-[#e15f2a] px-4 py-3 text-center font-black">Report problem</Link></div></aside></div></main>;
}

export function RegisterView() {
  return <main className="min-h-screen bg-[#f3f6f4]"><PublicHeader /><div className="mx-auto max-w-xl px-5 py-14"><section className="rounded-[2rem] border border-[#dfe6e1] bg-white p-7 shadow-lg"><h1 className="text-3xl font-black">Create citizen account</h1><p className="mt-2 text-sm text-[#66736c]">Optional for anonymous reports. An account lets you see every report in one place.</p><form className="mt-7 space-y-4" onSubmit={(event) => event.preventDefault()}>{[["Full name","text"],["Email","email"],["Password","password"]].map(([label,type]) => <label key={label} className="block text-sm font-bold">{label}<input type={type} className="mt-2 w-full rounded-xl border border-[#c9d4ce] px-4 py-3.5 outline-none focus:border-[#0b5d45]" /></label>)}<button className="w-full rounded-xl bg-[#0b5d45] px-5 py-4 font-black text-white">Create account</button></form><p className="mt-5 text-center text-sm">Already have access? <Link href="/login" className="font-bold text-[#0b5d45]">Demo login</Link></p></section></div></main>;
}
